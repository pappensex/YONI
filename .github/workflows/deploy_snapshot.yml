name: 📦 YONI Deploy Snapshot

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  snapshot:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Generate daily snapshot file
        id: snapshot
        run: |
          mkdir -p Transzendenz/Reports
          DATE=$(date +'%Y-%m-%d')
          TS=$(date +'%Y-%m-%d %H:%M:%S')
          cp Transzendenz/Reports/Deploy-Status.md Transzendenz/Reports/Deploy-Status-${DATE}.md
          echo "✅ Snapshot for ${DATE} created at ${TS}" >> Transzendenz/Reports/snapshot-log.txt
          echo "DATE=${DATE}" >> $GITHUB_ENV
          echo "TS=${TS}" >> $GITHUB_ENV
          echo "REPO=${{ github.repository }}" >> $GITHUB_ENV

      - name: Commit and Push Snapshot
        run: |
          git config --global user.name "YONI AutoBot"
          git config --global user.email "bot@pihoch2.me"
          git add Transzendenz/Reports/
          git commit -m "🕓 Daily Snapshot $(date +'%Y-%m-%d')"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Render HTML Email
        id: render
        run: |
          cat <<'EOF' > email.html
          <!DOCTYPE html>
          <html lang="de">
            <head>
              <meta charset="UTF-8" />
              <style>
                body {
                  font-family: "Helvetica Neue", Arial, sans-serif;
                  background-color: #000;
                  color: #f8f5e1;
                  margin: 0;
                  padding: 0;
                }
                .container {
                  max-width: 600px;
                  margin: 40px auto;
                  background: #111;
                  border: 1px solid #333;
                  border-radius: 16px;
                  padding: 32px;
                }
                h1 {
                  color: #f8f5e1;
                  font-size: 22px;
                  text-align: center;
                  letter-spacing: 1px;
                }
                .badge {
                  display: inline-block;
                  padding: 6px 14px;
                  border-radius: 24px;
                  font-size: 14px;
                  font-weight: bold;
                  color: #000;
                  background: #f8d14e;
                  margin-bottom: 24px;
                }
                p {
                  line-height: 1.6;
                  color: #ccc;
                }
                a {
                  color: #f8d14e;
                  text-decoration: none;
                }
                footer {
                  margin-top: 30px;
                  font-size: 12px;
                  text-align: center;
                  color: #666;
                }
              </style>
            </head>
            <body>
              <div class="container">
                <h1>📦 YONI Deploy Snapshot</h1>
                <div style="text-align:center">
                  <span class="badge">Erfolgreich abgeschlossen</span>
                </div>
                <p>Der tägliche Snapshot wurde erfolgreich erstellt und in dein Repository übertragen:</p>

                <p>
                  👉 <a href="https://github.com/${{ github.repository }}/tree/main/Transzendenz/Reports/Deploy-Status-${{ env.DATE }}.md">
                    Transzendenz / Reports / Deploy-Status-${{ env.DATE }}.md
                  </a>
                </p>

                <p>
                  Zeitstempel: <strong>${{ env.TS }}</strong><br />
                  Repository: <strong>${{ github.repository }}</strong>
                </p>

                <footer>
                  © 2025 piHOCH2 · Autonom. Präzise. Perfekt.<br />
                  YONI AutoBot — daily system integrity check.
                </footer>
              </div>
            </body>
          </html>
          EOF

      - name: Send Email Notification
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 587
          username: ${{ secrets.MAIL_USER }}
          password: ${{ secrets.MAIL_PASS }}
          subject: "📦 YONI Deploy Snapshot — ${{ github.repository }} @ ${{ env.DATE }}"
          to: ${{ secrets.MAIL_TO }}
          from: "YONI AutoBot <bot@pihoch2.me>"
          content_type: text/html
          body: ${{ steps.render.outputs.body }}
